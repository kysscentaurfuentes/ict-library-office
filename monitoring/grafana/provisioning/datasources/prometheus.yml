# ICT-LIBRARY-OFFICE/monitoring/grafana/provisioning/datasources/prometheus.yml

apiVersion: 1

datasources:

  # =========================================================
  # PROMETHEUS
  # =========================================================
  - name: ICT Library Office

    type: prometheus

    access: proxy

    url: http://prometheus:9090

    isDefault: true

    editable: true