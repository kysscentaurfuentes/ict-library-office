# ICT-LIBRARY-OFFICE/monitoring/prometheus.yml
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "ict-library-backend"

    static_configs:
      - targets:
          - "host.docker.internal:4000"