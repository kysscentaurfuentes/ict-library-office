# ICT-LIBRARY-OFFICE/nginx/default.conf
server {
    listen 80;

    location / {
        proxy_pass http://frontend:5173;
    }

    location /api/ {
        proxy_pass http://backend:4000;
    }

    location /graphql {
        proxy_pass http://backend:4000/graphql;
    }
}